# Repository discovery tool boundary

The implementation must call the configured mob-code search connector and
write normalized results into the active change's `context/discovery/` files.
Do not hardcode repository names or local paths here.
