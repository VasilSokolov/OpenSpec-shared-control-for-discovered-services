# Repository discovery

Use mob-code search to discover repositories, modules, ownership, branches,
languages, frameworks, build commands, test commands, and dependency edges.
Never infer a target from the current shell directory or hardcode repository
names. Save results under the active change's `context/discovery/` directory.
