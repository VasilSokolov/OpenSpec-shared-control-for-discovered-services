# Mobile.de OpenSpec workflow

OpenSpec is the only user-facing workflow. Use feature-based change IDs and
keep the complete change in the control repository. Jira keys are optional
metadata. Do not create a second `/mde` command namespace.
