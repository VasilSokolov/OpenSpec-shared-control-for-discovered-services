---
description: Verify the approved spec revision in the MDE control plane against task status, commit, contracts, tests, observability, security, deployment, and traceability evidence. Alias of /opsx:verify.
argument-hint: "[change id | task id]"
---

# /openspec:verify

Alias of `/opsx:verify` for the MDE OpenSpec SDLC control repository. The
canonical workflow is defined in `.claude/commands/opsx/verify.md`, the single
source of truth.

Read `.claude/commands/opsx/verify.md` now and execute it exactly, applying any
ARGUMENTS passed to this command. Do not duplicate, summarize, or diverge from
it — the two namespaces must stay identical on every governance gate.
