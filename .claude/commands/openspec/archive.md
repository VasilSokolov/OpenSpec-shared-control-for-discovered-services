---
description: Archive a completed OpenSpec change in the MDE control plane after all tasks, verification, approvals, and evidence are complete — control package only, caraoke worktree teardown stays manual. Alias of /opsx:archive.
argument-hint: "[change id]"
---

# /openspec:archive

Alias of `/opsx:archive` for the MDE OpenSpec SDLC control repository. The
canonical workflow is defined in `.claude/commands/opsx/archive.md`, the single
source of truth.

Read `.claude/commands/opsx/archive.md` now and execute it exactly, applying any
ARGUMENTS passed to this command. Do not duplicate, summarize, or diverge from
it — the two namespaces must stay identical on every governance gate.
