---
description: Open/resume an OpenSpec change in the MDE control plane — Jira (QCT) intake, mob-code-search discovery of Swifty/Caraoke service repos, caraoke-workspace worktree seam. Alias of /opsx:propose.
argument-hint: "[QCT ticket key | feature description]"
---

# /openspec:propose

Alias of `/opsx:propose` for the MDE OpenSpec SDLC control repository. The
canonical workflow — Git preflight, `QCT`-first Jira intake, mob-code-search
repository discovery, the source-first Figma/traceability gate, the
caraoke-workspace workset seam, and the automatic validation gates — is defined
in `.claude/commands/opsx/propose.md`, the single source of truth.

Read `.claude/commands/opsx/propose.md` now and execute it exactly, applying any
ARGUMENTS passed to this command. Do not duplicate, summarize, or diverge from
it — the two namespaces must stay identical on every governance gate.
